# Deployment steps have moved to DEPLOY.md, matching Hemingway's real
# confirmed workflow (git push to GitHub -> SSH pull on Hetzner ->
# systemd restart) rather than a single automated script. See DEPLOY.md.
